= turbo_frame_tag "pairings"
  p.strong = "Subject Player: #{@interactions[:subject]}"

  table.small-table
    tr
      th Mate
      th Numb
      th Pairings
    -@interactions[:mname].each_with_index do |m,i|
      tr
        td = m
        - dt = []
        - gm = []
        - @interactions[:mates][i].each do |a|
          - dt << link_to( a[-1],game_path(a[0]),class:'blue-link').html_safe
        td = dt.count
        td 
          - dt.each do |a|
            span.mr-2 = a
  br

